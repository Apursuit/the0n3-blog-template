---
title: 博客部署与使用指南（Windows）
date: 2026-03-22 13:59:28
permalink: /posts/blog-deployment/
tags:
    - 部署
categories: 博客
draft: false
sidebar: true
pin: 1
---

博客部署流程

> [!NOTE]
> 这篇文章讲解当前这个博客部署详细流程，面向windows用户，其他操作系统用户相信有能力解决环境配置问题，本文不做过多赘述。

博客依赖 php、composer、git，部署前需要先安装好环境。


## 00. scoop 包管理器安装


打开 PowerShell（建议以普通用户运行），执行：

过程中会提示是否修改执行策略，输入 Y 确认。

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
irm get.scoop.sh | iex
```

安装 PHP 和 Composer

```powershell
scoop install php composer git
```

![](/images/blog-deployment/0.png)

安装完成后，验证是否成功：

```powershell
php -v
composer -V
git -v
```

![](/images/blog-deployment/01.png)

至此，环境配置完成。

文章末尾附 手动安装并配置 流程。

通过 scoop 安装 php composer 的过程提示，可以知道 scoop 的运行原理（感兴趣可以深入了解）

Scoop 会自动处理：下载，解压，配置路径，创建命令入口

```powershell
PS C:\Users\demo> scoop install php composer
WARN  Purging previous failed installation of php.
ERROR 'php' isn't installed correctly.
Removing older version (8.5.4).
'php' was uninstalled.
Installing 'php' (8.5.4) [64bit] from 'main' bucket
Downloading https://downloads.php.net/~windows/releases/archives/php-8.5.4-Win32-vs17-x64.zip (-1 B)...

Checking hash of php-8.5.4-Win32-vs17-x64.zip ... ok.
Extracting php-8.5.4-Win32-vs17-x64.zip ... done.
Running pre_install script...done.
Linking ~\scoop\apps\php\current => ~\scoop\apps\php\8.5.4
Creating shim for 'php'.
Creating shim for 'php-cgi'.
Creating shim for 'phpdbg'.
Persisting cli
Persisting php.ini-production
Running post_install script...done.
'php' (8.5.4) was installed successfully!
Installing 'composer' (2.9.5) [64bit] from 'main' bucket
composer.phar (3.1 MB) [======================================================================================] 100%
Checking hash of composer.phar ... ok.
Running pre_install script...done.
Linking ~\scoop\apps\composer\current => ~\scoop\apps\composer\2.9.5
Creating shim for 'composer'.
Adding ~\scoop\apps\composer\current\home\vendor\bin to your path.
Persisting home
Persisting cache
'composer' (2.9.5) was installed successfully!
Notes
-----
'composer selfupdate' is aliased to 'scoop update composer'
'php' suggests installing 'extras/vcredist2022'.
```

## 01. 克隆项目到本地：

```bash
git clone https://hk.gh-proxy.org/https://github.com/Apursuit/the0n3-blog.git
cd the0n3-blog
```


## 02. composer 依赖安装

在博客根目录下，打开终端，输入以下命令安装依赖：

```bash
composer install
```

## 03. 开始写作

博客结构如下:



- assets/            前端资源（CSS/JS/Prism）
- config/            站点配置
- logs/              构建日志
- posts/             Markdown 文章（支持子目录）
- public/            静态资源（原样拷贝到 dist/）
- images/            图片资源（原样拷贝到 dist/images/）
- src/               核心 PHP 逻辑（解析、渲染、生成）
- templates/         页面模板
- dist/              构建输出（生成后出现）
- main.php           CLI 入口

其中，posts/ 目录下存放 Markdown 文章，可以直接在该目录下创建 Markdown 文件，也可以创建子目录来组织文章，例如 posts/2026/03/22/test.md。

Markdown 文章需要包含 YAML Front Matter，格式如下：

```markdown
title: Hello World
date: 2026-03-20
permalink: /posts/hello-world/
tags:
	- 示例
categories:
	- 示例
pin: 0
draft: false
sidebar: true
```

可以通过运行 main.php 获取新文章模板：

```bash
php main.php new "文章标题"
```

这会打印一份包含 Front Matter 模板的 Markdown 内容，你可以复制到新的 Markdown 文件中，并根据需要修改字段值。

**注意，--- 分隔符必须顶格、并且必须出现在文件开头**

![](/images/blog-deployment/13.png)

如果文章包含图片资源，可以放在 images 目录下，例如 images/testdir/1.png，然后在 Markdown 中使用相对路径引用：

```markdown
![测试图片](/images/testdir/1.png)
```

文章书写完成后，运行构建命令：

```bash
php main.php build
```

博客构建的产物在 dist目录，开启 web 服务，访问 http://localhost:8000 预览效果：

```bash
php -S localhost:8000 -t dist/
```

## 04. 推送博客

github 配置 ssh 公钥，参考 https://www.qfxaile.top/index.php/archives/120.html

配置本地 git 仓库，关联远程仓库地址，提交并推送代码到远程仓库。

```bash
git init
git add .
git commit -m "first commit"
git remote add origin <远程仓库地址>
git push -u origin main
```

推送到远程仓库后，GitHub Action 会自动触发构建流程，构建完成后会将 dist 目录下的静态文件推送到 gh-pages 分支，实现自动部署。


## 05. Giscus 评论系统

默认关闭。如需启用，请在 config/site.php 的 giscus 配置中填入你自己的参数（repo / repo_id / category / category_id）。

重要：不要直接使用他人的配置，否则评论会写入对方仓库。

注意：启用 Giscus 评论系统需要在 GitHub 仓库创建 Discussions，具体步骤参考这篇文章 https://www.lixueduan.com/posts/blog/02-add-giscus-comment/



## 附录：手动安装并配置

### PHP安装

下载地址 https://downloads.php.net/~windows/releases/archives/

选择 php-8.5.0-Win32-vs17-x64.zip

![](/images/blog-deployment/1.png)

下载后，解压压缩包到指定目录，这里演示解压到 C:\php-8.5.4，你可以解压到任何目录，如 D:\php-8.5.4 等。

![](/images/blog-deployment/2.png)

### 环境变量配置

按 win 键，输入环境变量，选择 编辑系统环境变量

![](/images/blog-deployment/3.png)

![](/images/blog-deployment/4.png)

选中用户的 Path 变量，点击编辑，点击新建，输入 php 的解压目录，例如 C:\php-8.5.4\，点击确定保存。

![](/images/blog-deployment/5.png)

![](/images/blog-deployment/6.png)


验证是否成功，打开终端（按 win 键输入终端或cmd），输入 php -v，如果显示 PHP 版本信息，则说明配置成功。

![](/images/blog-deployment/7.png)

php 需要启用 zip 扩展，打开 php.ini 文件（在 php 解压目录下），找到 extension=zip，去掉前面的分号 ; 以启用该扩展，保存文件。

![](/images/blog-deployment/12.png)


### composer 安装

下载地址：https://getcomposer.org/download/ 

![](/images/blog-deployment/8.png)


下载后，运行安装程序，选择安装路径（可以放在任意目录），点击安装.

composer 安装过程会检测你的 PHP 环境，如果之前的环境变量配置正确，安装程序会自动找到 PHP 可执行文件。

![](/images/blog-deployment/9.png)


不使用代理，留空，点击继续，后续默认完成安装。

![](/images/blog-deployment/10.png)

打开终端，输入 composer ，如果显示 composer 相关帮助信息，则说明安装成功。

![](/images/blog-deployment/11.png)
